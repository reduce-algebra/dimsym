%out "output.burgers"$

%burgers

%load packages
load dimsym23$

loaddeq ( u(1,1,1) = u(1,2) - u(1,1)**2); % burgers equation;

mkdets(point);  
solvedets(std);
showdets();
mkgens();
stats();

%shut "output.burgers"$

end;
